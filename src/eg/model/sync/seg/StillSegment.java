package eg.model.sync.seg;

import eg.model.sync.SyncBlockSet;
import eg.model.sync.SyncSegment;

public final class StillSegment extends SyncSegment {
	
	public StillSegment(SyncBlockSet blockSet) {
		super(blockSet);
	}
}
